#db
spring:
    datasource:
      url: jdbc:postgresql://localhost:5432/demo?characterEncoding=UTF-8&useUnicode=true&autoReconnect=true
      username: root
      password:
    jpa:
      generate-ddl: true
      database-platform: org.hibernate.dialect.PostgreSQLDialect
      hibernate:
        ddl-auto: create
    data:
      rest:
        base-path: /api

#app
server.port: 8084
